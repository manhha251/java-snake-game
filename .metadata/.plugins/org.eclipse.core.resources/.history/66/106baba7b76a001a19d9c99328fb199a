package snake;

import java.awt.Color;

public enum Tile {
	SNAKE_HEAD(Color.yellow),
	SNAKE_BODY(Color.green),
	APPLE(Color.red);
	
	Tile(Color color) {
		this.color = color;
	}
	
	final Color color;
}
